# PROMPT_00_INITIAL_BOILERPLATE.md

Act as a senior software architect and pair programmer.

I want to understand everything being created.

Build incrementally and explain every file and command.

Do not generate the full RAG application.

Generate only the initial boilerplate with health checks.

Stop after completing this stage and wait for my confirmation.

---

# Existing Repository

I already have:

```text
RAG/
```

Use it as the root directory.

---

# Technology Stack

Frontend

* React
* JavaScript
* Vite

Backend

* Python
* FastAPI
* Uvicorn

Database

* Qdrant

Containerization

* Docker
* Docker Compose

---

# Goal

When I launch the application in Chrome:

1. React application should load.
2. A button called:

```text
Check Health
```

should be visible.

3. Clicking the button should call:

```http
GET /health
```

on FastAPI.

4. FastAPI should verify that Qdrant is reachable.

5. Return:

```json
{
    "backend": true,
    "qdrant": true
}
```

or

```json
{
    "backend": true,
    "qdrant": false
}
```

6. React should display the result on screen.

---

# Repository Structure

Create:

```text
RAG/

backend/

frontend/

database/

storage/

docker-compose.yml

.env

.env.example

setup.sh
```

---

# Storage

Create:

```text
storage/

primary_user/
```

Assume only one user.

Multi-user support will be added later.

---

# Database

Create:

```text
database/

qdrant_data/
```

for persistence.

---

# Backend

Create only the files necessary.

Example:

```text
backend/

main.py

requirements.txt

Dockerfile
```

Do not create:

* api/
* routers/
* services/
* models/
* embeddings/
* rerankers/
* llms/

yet.

---

# Backend API

Implement:

### GET /

Returns:

```json
{
    "message": "Chat With Your Docs API"
}
```

---

### GET /health

This endpoint should:

1. Verify backend is running.
2. Connect to Qdrant.
3. Determine whether Qdrant is healthy.

Return:

```json
{
    "backend": true,
    "qdrant": true
}
```

or

```json
{
    "backend": true,
    "qdrant": false
}
```

Handle exceptions gracefully.

---

# Qdrant

Use the official container:

```text
qdrant/qdrant
```

Expose:

```text
6333
```

Persist data inside:

```text
database/qdrant_data
```

using Docker volumes.

---

# Frontend

Use:

* React
* JavaScript
* Vite

Do NOT use TypeScript.

Create only the files necessary.

No components folder.

No hooks folder.

No pages folder.

No CSS frameworks.

---

# UI

Display:

```text
Chat With Your Docs
```

and a button:

```text
Check Health
```

On click:

Call:

```http
GET /health
```

Display:

```text
Backend : Healthy

Qdrant : Healthy
```

or

```text
Backend : Healthy

Qdrant : Unreachable
```

Use simple state management.

---

# Docker

Create Dockerfiles for:

### Backend

Use:

```text
python:3.12-slim
```

Run:

```bash
uvicorn main:app --host 0.0.0.0 --reload
```

---

### Frontend

Use:

```text
node:lts
```

Expose:

```text
3000
```

---

# Docker Compose

Create three services:

### frontend

Port:

3000

---

### backend

Port:

8000

Depends on:

qdrant

---

### qdrant

Port:

6333

Mount:

```text
./database/qdrant_data:/qdrant/storage
```

All services should restart automatically.

---

# Environment Variables

Create:

.env.example

with:

```env
BACKEND_PORT=8000

FRONTEND_PORT=3000

QDRANT_HOST=qdrant

QDRANT_PORT=6333
```

Create .env from it.

---

# setup.sh

Responsibilities:

1. Check Docker.

2. Check Docker Compose.

3. Check Node installation.

4. Create .env if missing.

5. Create:

```text
database/qdrant_data

storage/primary_user
```

if missing.

6. Execute:

```bash
docker compose up --build
```

---

# Important

I want to understand the project.

Explain:

* every file created
* every dependency added
* every Docker container
* every command

Do not move to uploads, chunking, embeddings, rerankers or LLMs.

Stop after this stage and wait for my confirmation.
